# 数学表达式识别与计算系统技术文档

## 项目概述

本项目是一个基于OCR技术的数学表达式识别与计算系统，能够从图片中识别数学表达式并进行安全计算。系统专门针对加减乘除和括号运算进行优化，具有良好的安全性和准确性。

## 系统架构

### 核心组件
- **OCR引擎**: PaddleOCR (中文优化)
- **表达式处理**: 符号标准化 + 格式验证
- **安全计算**: 表达式验证 + 安全执行

### 技术栈
- Python 3.x
- PaddleOCR
- 正则表达式
- 安全计算机制

## 核心函数详解

### 1. `normalize_expr(expr)` - 表达式标准化函数

**功能**: 将各种格式的数学符号统一转换为标准格式

```python
def normalize_expr(expr):
    # 中文符号转英文符号
    expr = expr.replace('（', '(').replace('）', ')')
    expr = expr.replace('＋', '+').replace('－', '-').replace('＊', '*').replace('／', '/')
    
    # 特殊数学符号转换
    expr = expr.replace('×', '*').replace('÷', '/')
    
    # 格式清理
    expr = expr.replace(' ', '').replace('\n', '')
    return expr
```

**支持的符号转换**:
- 括号: `（` → `(`，`）` → `)`
- 运算符: `＋` → `+`，`－` → `-`，`＊` → `*`，`／` → `/`
- 数学符号: `×` → `*`，`÷` → `/`

### 2. `extract_expression(image_path)` - 表达式提取函数

**功能**: 从图片中提取并验证数学表达式

```python
def extract_expression(image_path):
    # OCR识别 → 文本提取 → 合并处理 → 格式验证 → 返回结果
```

**处理流程**:
1. **OCR识别**: 使用PaddleOCR识别图片中的文本
2. **文本提取**: 提取所有识别到的文本行
3. **合并处理**: 将所有文本行合并为一个字符串
4. **符号标准化**: 调用`normalize_expr`进行符号统一
5. **格式验证**: 使用正则表达式验证表达式格式
6. **结果返回**: 返回符合格式的表达式或None

**正则表达式模式**:
```python
r'^[\d+\-*/()]+$'  # 只允许数字、加减乘除和括号
```

### 3. `calc_expression(expr)` - 表达式计算函数

**功能**: 安全计算数学表达式

```python
def calc_expression(expr):
    # 安全验证 → 表达式计算 → 结果返回
```

**安全机制**:
1. **格式验证**: 使用正则表达式验证表达式安全性
2. **异常处理**: 捕获所有可能的计算异常
3. **错误信息**: 提供清晰的错误提示

## 系统特性

### 安全性
- **白名单验证**: 只允许特定的数学符号
- **异常处理**: 完善的错误捕获机制
- **拒绝恶意输入**: 防止代码注入攻击

### 兼容性
- **多符号支持**: 支持中文和英文数学符号
- **格式容错**: 自动处理空格和换行符
- **OCR适应性**: 针对OCR识别错误进行优化

### 准确性
- **双重验证**: 先合并验证，再单行验证
- **符号标准化**: 统一不同格式的数学符号
- **错误提示**: 详细的处理过程输出

## 使用示例

### 基本使用
```python
# 识别图片中的表达式
image_path = 'q/5.png'
expr = extract_expression(image_path)

if expr:
    result = calc_expression(expr)
    print(f"表达式: {expr} = {result}")
else:
    print("未找到有效表达式")
```

### 支持的表达式格式